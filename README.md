
# mini-project-submission
Solution:

Restarted Minikube:

minikube stop
minikube start


## Verification

All required tools were verified using:

git --version
python3 --version
java --version
docker --version
docker compose version
kubectl version --client
terraform version
ansible --version
helm version
minikube version
az version
aws --version
 (Initial mini project submission)
